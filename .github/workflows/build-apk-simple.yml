name: Build Android APK (Simple)

on:
  push:
    tags:
      - 'v*'  # Только при создании тега версии
  workflow_dispatch:  # Ручной запуск

jobs:
  build-apk:
    name: Build APK with Official Kivy Docker
    runs-on: ubuntu-latest
    timeout-minutes: 90
    
    env:
      DOCKER_BUILDKIT: 0
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
      
      - name: � Pull Buildozer Docker image
        run: |
          docker pull kivy/buildozer:master
      
      - name: �🔨 Build APK with Buildozer in Docker
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/home/user/hostcwd" \
            kivy/buildozer:master \
            bash -c "cd /home/user/hostcwd && buildozer -v android debug"
      
      - name: 📋 Show build results
        run: |
          echo "=== Build completed ==="
          if [ -d "bin" ]; then
            echo "APK files found:"
            ls -lh bin/*.apk || echo "No APK files"
          else
            echo "⚠️ bin directory not found"
          fi
      
      - name: 📤 Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: snake-game-apk
          path: bin/*.apk
          retention-days: 30
      
      - name: 🎉 Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: bin/*.apk
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
