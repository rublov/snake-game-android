"""
Простой скрипт для создания фиктивного теста.

Используется для проверки работы скрипта skip_failing_tests.py
"""


def test_dummy():
    """Простой тест, который всегда проходит."""
    # Используем условие вместо assert для лучшей совместимости
    condition = True
    if not condition:
        raise ValueError("Этот тест всегда должен проходить")


if __name__ == "__main__":
    print("Это фиктивный тестовый файл для проверки")
